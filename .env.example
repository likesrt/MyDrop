## 端口配置（简化）
# PORT: 应用监听端口（容器/进程内），默认 3000。
PORT=3000

# PUBLISH_PORT（Docker Compose）：主机端口，映射为 主机端口:容器端口(3000)。
# 例如仅修改主机端口：
# PUBLISH_PORT=8080   # 映射为 8080:3000
# 如需仅绑定到 127.0.0.1，请直接按 docker-compose.yml 中注释修改对应行。

## 上传与配额限制
# FILE_SIZE_LIMIT_MB: 单个文件大小上限（单位 MB，默认 5）。
# 超出将被拒绝上传。
FILE_SIZE_LIMIT_MB=20

# MAX_FILES: 全局最多可保留的文件数量上限（默认 10）。
# 达到上限后继续上传会失败，或需先删除旧文件。
MAX_FILES=1000

## 日志配置
# LOG_LEVEL: 日志级别（error | warn | info | debug），默认 info。
LOG_LEVEL=warn

# LOG_FILE: 可选，日志文件输出路径。如需输出到文件，取消注释并指定路径。
# 例如：LOG_FILE=logs/app.log
LOG_FILE=logs/app.log

# LOG_MAX_SIZE_MB: 单个日志文件最大尺寸（MB）。超过后会自动切割为历史日志，重新生成最新日志文件（默认 5）。
LOG_MAX_SIZE_MB=5

# LOG_ROTATE_KEEP: 历史日志保留数量（默认 5）。超过数量的旧日志会被自动清理。
LOG_ROTATE_KEEP=5

## SQLite 配置（可选）
# SQLITE_JOURNAL_MODE: SQLite 日志模式（WAL | DELETE | TRUNCATE | MEMORY）。
# 默认 WAL；若运行环境（如某些网络共享卷/NFS）不支持 WAL，可改为 DELETE 以避免启动失败反复重启。
# SQLITE_JOURNAL_MODE=WAL

## 鉴权与会话
# JWT_SECRET: JWT 签名密钥（必须强设置！默认示例值仅用于开发）。
# 生产环境务必改为高强度随机字符串，并妥善保管切勿泄露。
JWT_SECRET="Starter-Twiddling-Glacier-Washout8-Pegboard-Unharmed-Snugly-Laborer"

# JWT_EXPIRES_DAYS: JWT 过期天数（默认 7 天）。
# 到期需重新登录。
JWT_EXPIRES_DAYS=7

## 前端行为开关
# HEADER_AUTO_HIDE: 移动端自动隐藏顶部设置栏（0 关闭，1/true/yes 开启）。
# 开启后：
# - 移动端页面顶部 50px 为唤起区域，点击/触碰可显示设置栏；
# - 10 秒无交互自动平滑隐藏；
HEADER_AUTO_HIDE=0

## PWA / 静态资源版本（用于 Service Worker 缓存更新）
# ASSET_VERSION: 以查询字符串 ?v=<版本> 的形式控制静态资源更新。
# 不设置则默认使用 package.json 的 version；生产环境建议与构建版本保持一致。
# 仅影响 Service Worker 的缓存键，不影响实际文件内容。
# 例如：ASSET_VERSION=2025.09.23
# ASSET_VERSION=

## 自动清理（过期消息/文件 & 长期未活跃设备）
# AUTO_CLEANUP_ENABLED: 是否启用周期性清理（true/false）。默认 true。
# AUTO_CLEANUP_ENABLED=true

# CLEANUP_INTERVAL_MINUTES: 清理任务执行间隔（分钟）。默认 720。
# CLEANUP_INTERVAL_MINUTES=720

# MESSAGE_TTL_DAYS: 自动删除早于 N 天的消息与其关联文件（0 表示禁用）。默认 0。
# MESSAGE_TTL_DAYS=0

# DEVICE_INACTIVE_DAYS: 删除 N 天未活跃的设备（0 表示禁用）。默认 0。
# DEVICE_INACTIVE_DAYS=0
